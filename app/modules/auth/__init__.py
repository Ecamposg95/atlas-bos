"""Atlas BOS module - auth.

S1 placeholder: this package will own the auth domain (router, schemas,
services, dependencies for login, JWT, password reset, sessions). Today
the implementation still lives at the legacy paths (app/routers/auth.py,
app/utils/auth.py, app/dependencies.py auth helpers); migration to this
package happens in S1 of Phase 2.

See context/PHASE_2_BACKEND_MODULARIZATION.md.
"""
