"""Atlas BOS module - users.

S1 placeholder: this package will own the users domain (router, schemas,
services, models for user CRUD, profile, roles, invitations). Today the
implementation still lives at the legacy paths (app/routers/users.py,
app/models/users.py, app/crud/users.py, app/schemas/users.py); migration
to this package happens in S1 of Phase 2.

See context/PHASE_2_BACKEND_MODULARIZATION.md.
"""
