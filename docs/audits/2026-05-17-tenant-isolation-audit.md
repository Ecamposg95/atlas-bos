# Multi-tenant isolation audit — 2026-05-17

Generated by `scripts/audit_tenant_isolation.py`. Enforces docs/modules/MODULE_GUIDE.md §6 (Multi-tenancy obligatorio): every business query MUST filter by `organization_id` or `tenant_id`.

## Summary

**Summary:** scanned 747 queries — 454 safe, 187 suspect, 106 unclassified. Coverage: 60.8%.

- Total `db.query(...)` chains scanned: **747**
- Safe (filters by tenant or whitelisted model): **454**
- Suspect (no tenant filter): **187**
- Unclassified (AST couldn't decide): **106**
- Coverage: **60.8%**

Whitelist (treated as legitimately tenant-less): `ApiKey`, `Branch`, `Brand`, `Department`, `FeatureFlag`, `IndustryPreset`, `Module`, `Organization`, `OrganizationModule`, `PlatformAlert`, `PlatformAnnouncement`, `PlatformAuditLog`, `PlatformIncident`, `PrintJob`, `UnitOfMeasure`, `User`

## Suspect queries by file

### `app/routers/platform/branches.py`  (30 suspect)

- **app/routers/platform/branches.py:48** — model `SalesDocument`
    - Filter: `"sales": db.query(SalesDocument).filter(SalesDocument.branch_id == branch_id).count(),`
    - Sugerencia: reemplazar por `get_tenant_scoped(db, SalesDocument, <id>, current_user)`
- **app/routers/platform/branches.py:49** — model `CashSession`
    - Filter: `"cash_sessions": db.query(CashSession).filter(CashSession.branch_id == branch_id).count(),`
    - Sugerencia: reemplazar por `get_tenant_scoped(db, CashSession, <id>, current_user)`
- **app/routers/platform/branches.py:140** — model `SalesDocument`
    - Filter: `sales_count = db.query(SalesDocument).filter(SalesDocument.branch_id == branch_id).count()`
    - Sugerencia: reemplazar por `get_tenant_scoped(db, SalesDocument, <id>, current_user)`
- **app/routers/platform/branches.py:143** — model `CashSession`
    - Filter: `cash_count = db.query(CashSession).filter(CashSession.branch_id == branch_id).count()`
    - Sugerencia: reemplazar por `get_tenant_scoped(db, CashSession, <id>, current_user)`
- **app/routers/platform/branches.py:194** — model `SaleReturn`
    - Filter: `deleted["sale_returns_branch"] = db.query(SaleReturn).filter( SaleReturn.branch_id == branch_id ).delete(synchronize_session=False)`
    - Sugerencia: reemplazar por `get_tenant_scoped(db, SaleReturn, <id>, current_user)`
- **app/routers/platform/branches.py:198** — model `SalesDocument`
    - Filter: `deleted["sales"] = db.query(SalesDocument).filter( SalesDocument.branch_id == branch_id ).delete(synchronize_session=False)`
    - Sugerencia: reemplazar por `get_tenant_scoped(db, SalesDocument, <id>, current_user)`
- **app/routers/platform/branches.py:220** — model `ProductBranchStatus`
    - Filter: `deleted["product_branch_status"] = db.query(ProductBranchStatus).filter( ProductBranchStatus.branch_id == branch_id ).delete(synchronize_session=False)`
    - Sugerencia: reemplazar por `get_tenant_scoped(db, ProductBranchStatus, <id>, current_user)`
- **app/routers/platform/branches.py:225** — model `InventoryMovement`
    - Filter: `deleted["inventory_movements"] = db.query(InventoryMovement).filter( (InventoryMovement.branch_id == branch_id) | (InventoryMovement.from_branch_id == branch_id) | (InventoryMov...`
    - Sugerencia: reemplazar por `get_tenant_scoped(db, InventoryMovement, <id>, current_user)`
- **app/routers/platform/branches.py:230** — model `StockOnHand`
    - Filter: `deleted["stock_on_hand"] = db.query(StockOnHand).filter( StockOnHand.branch_id == branch_id ).delete(synchronize_session=False)`
    - Sugerencia: reemplazar por `get_tenant_scoped(db, StockOnHand, <id>, current_user)`
- **app/routers/platform/branches.py:235** — model `PurchaseRecommendation`
    - Filter: `deleted["purchase_recommendations"] = db.query(PurchaseRecommendation).filter( PurchaseRecommendation.branch_id == branch_id ).delete(synchronize_session=False)`
    - Sugerencia: reemplazar por `get_tenant_scoped(db, PurchaseRecommendation, <id>, current_user)`
- **app/routers/platform/branches.py:267** — model `Attendance`
    - Filter: `deleted["attendances"] = db.query(Attendance).filter( Attendance.branch_id == branch_id ).delete(synchronize_session=False)`
    - Sugerencia: reemplazar por `get_tenant_scoped(db, Attendance, <id>, current_user)`
- **app/routers/platform/branches.py:270** — model `BranchAssignment`
    - Filter: `deleted["branch_assignments"] = db.query(BranchAssignment).filter( BranchAssignment.branch_id == branch_id ).delete(synchronize_session=False)`
    - Sugerencia: reemplazar por `get_tenant_scoped(db, BranchAssignment, <id>, current_user)`
- **app/routers/platform/branches.py:275** — model `Employee`
    - Filter: `db.query(Employee).filter(Employee.base_branch_id == branch_id).update( {"base_branch_id": None}, synchronize_session=False )`
    - Sugerencia: reemplazar por `get_tenant_scoped(db, Employee, <id>, current_user)`
- **app/routers/platform/branches.py:280** — model `Expense`
    - Filter: `db.query(Expense).filter(Expense.branch_id == branch_id).update( {"branch_id": None}, synchronize_session=False )`
    - Sugerencia: reemplazar por `get_tenant_scoped(db, Expense, <id>, current_user)`
- **app/routers/platform/branches.py:283** — model `PurchaseOrder`
    - Filter: `db.query(PurchaseOrder).filter(PurchaseOrder.branch_id == branch_id).update( {"branch_id": None}, synchronize_session=False )`
    - Sugerencia: reemplazar por `get_tenant_scoped(db, PurchaseOrder, <id>, current_user)`
- **app/routers/platform/branches.py:186** — model `Payment`
    - Filter: `deleted["payments"] = db.query(Payment).filter( Payment.sales_document_id.in_(sale_ids) ).delete(synchronize_session=False)`
    - Sugerencia: reemplazar por `get_tenant_scoped(db, Payment, <id>, current_user)`
- **app/routers/platform/branches.py:190** — model `SalesLineItem`
    - Filter: `deleted["sales_lines"] = db.query(SalesLineItem).filter( SalesLineItem.document_id.in_(sale_ids) ).delete(synchronize_session=False)`
    - Sugerencia: reemplazar por `get_tenant_scoped(db, SalesLineItem, <id>, current_user)`
- **app/routers/platform/branches.py:207** — model `CashMovement`
    - Filter: `deleted["cash_movements"] = db.query(CashMovement).filter( CashMovement.session_id.in_(session_ids) ).delete(synchronize_session=False)`
    - Sugerencia: reemplazar por `get_tenant_scoped(db, CashMovement, <id>, current_user)`
- **app/routers/platform/branches.py:211** — model `SaleReturn`
    - Filter: `db.query(SaleReturn).filter(SaleReturn.cash_session_id.in_(session_ids)).update( {"cash_session_id": None}, synchronize_session=False )`
    - Sugerencia: reemplazar por `get_tenant_scoped(db, SaleReturn, <id>, current_user)`
- **app/routers/platform/branches.py:215** — model `CashSession`
    - Filter: `deleted["cash_sessions"] = db.query(CashSession).filter( CashSession.id.in_(session_ids) ).delete(synchronize_session=False)`
    - Sugerencia: reemplazar por `get_tenant_scoped(db, CashSession, <id>, current_user)`
- **app/routers/platform/branches.py:249** — model `TransferFulfillmentLine`
    - Filter: `deleted["transfer_fulfillment_lines"] = db.query(TransferFulfillmentLine).filter( TransferFulfillmentLine.fulfillment_id.in_(ful_ids) ).delete(synchronize_session=False)`
    - Sugerencia: reemplazar por `get_tenant_scoped(db, TransferFulfillmentLine, <id>, current_user)`
- **app/routers/platform/branches.py:253** — model `TransferFulfillment`
    - Filter: `deleted["transfer_fulfillments"] = db.query(TransferFulfillment).filter( TransferFulfillment.id.in_(ful_ids) ).delete(synchronize_session=False)`
    - Sugerencia: reemplazar por `get_tenant_scoped(db, TransferFulfillment, <id>, current_user)`
- **app/routers/platform/branches.py:258** — model `TransferOrderLine`
    - Filter: `deleted["transfer_order_lines"] = db.query(TransferOrderLine).filter( TransferOrderLine.transfer_id.in_(transfer_ids) ).delete(synchronize_session=False)`
    - Sugerencia: reemplazar por `get_tenant_scoped(db, TransferOrderLine, <id>, current_user)`
- **app/routers/platform/branches.py:262** — model `TransferOrder`
    - Filter: `deleted["transfer_orders"] = db.query(TransferOrder).filter( TransferOrder.id.in_(transfer_ids) ).delete(synchronize_session=False)`
    - Sugerencia: reemplazar por `get_tenant_scoped(db, TransferOrder, <id>, current_user)`
- **app/routers/platform/branches.py:170** — model `SalesDocument`
    - Filter: `sale_ids = [r[0] for r in db.query(SalesDocument.id).filter( SalesDocument.branch_id == branch_id ).all()]`
    - Sugerencia: reemplazar por `get_tenant_scoped(db, SalesDocument, <id>, current_user)`
- **app/routers/platform/branches.py:178** — model `SaleReturnItem`
    - Filter: `deleted["sale_return_items"] = db.query(SaleReturnItem).filter( SaleReturnItem.return_id.in_(return_ids) ).delete(synchronize_session=False)`
    - Sugerencia: reemplazar por `get_tenant_scoped(db, SaleReturnItem, <id>, current_user)`
- **app/routers/platform/branches.py:182** — model `SaleReturn`
    - Filter: `deleted["sale_returns"] = db.query(SaleReturn).filter( SaleReturn.id.in_(return_ids) ).delete(synchronize_session=False)`
    - Sugerencia: reemplazar por `get_tenant_scoped(db, SaleReturn, <id>, current_user)`
- **app/routers/platform/branches.py:203** — model `CashSession`
    - Filter: `session_ids = [r[0] for r in db.query(CashSession.id).filter( CashSession.branch_id == branch_id ).all()]`
    - Sugerencia: reemplazar por `get_tenant_scoped(db, CashSession, <id>, current_user)`
- **app/routers/platform/branches.py:240** — model `TransferOrder`
    - Filter: `transfer_ids = [r[0] for r in db.query(TransferOrder.id).filter( TransferOrder.requesting_branch_id == branch_id ).all()]`
    - Sugerencia: reemplazar por `get_tenant_scoped(db, TransferOrder, <id>, current_user)`
- **app/routers/platform/branches.py:174** — model `SaleReturn`
    - Filter: `return_ids = [r[0] for r in db.query(SaleReturn.id).filter( SaleReturn.sale_id.in_(sale_ids) ).all()]`
    - Sugerencia: reemplazar por `get_tenant_scoped(db, SaleReturn, <id>, current_user)`

### `app/routers/cash.py`  (23 suspect)

- **app/routers/cash.py:34** — model `CashSession`
    - Filter: `session = db.query(CashSession).filter( CashSession.user_id == current_user.id, CashSession.branch_id == current_user.branch_id, CashSession.status == CashSessionStatus.OPEN ).f...`
    - Sugerencia: reemplazar por `get_tenant_scoped(db, CashSession, <id>, current_user)`
- **app/routers/cash.py:52** — model `CashSession`
    - Filter: `return db.query(CashSession).filter( CashSession.branch_id == current_user.branch_id ).order_by(CashSession.opened_at.desc()).offset(skip).limit(limit).all()`
    - Sugerencia: reemplazar por `get_tenant_scoped(db, CashSession, <id>, current_user)`
- **app/routers/cash.py:70** — model `CashSession`
    - Filter: `active = db.query(CashSession).filter( CashSession.user_id == current_user.id, CashSession.branch_id == current_user.branch_id, CashSession.status == CashSessionStatus.OPEN ).fi...`
    - Sugerencia: reemplazar por `get_tenant_scoped(db, CashSession, <id>, current_user)`
- **app/routers/cash.py:84** — model `_Branch`
    - Filter: `branch = db.query(_Branch).filter(_Branch.id == current_user.branch_id).first()`
    - Sugerencia: reemplazar por `get_tenant_scoped(db, _Branch, <id>, current_user)`
- **app/routers/cash.py:123** — model `ParkedTicket`
    - Filter: `pending_parked = db.query(ParkedTicket).filter( ParkedTicket.user_id == session.user_id, ParkedTicket.branch_id == session.branch_id, ParkedTicket.created_at >= session.opened_a...`
    - Sugerencia: reemplazar por `get_tenant_scoped(db, ParkedTicket, <id>, current_user)`
- **app/routers/cash.py:214** — model `CashSession`
    - Filter: `session = db.query(CashSession).filter( CashSession.user_id == current_user.id, CashSession.branch_id == current_user.branch_id, CashSession.status == CashSessionStatus.OPEN ).f...`
    - Sugerencia: reemplazar por `get_tenant_scoped(db, CashSession, <id>, current_user)`
- **app/routers/cash.py:261** — model `CashSession`
    - Filter: `session = db.query(CashSession).filter( CashSession.id == payload.session_id, CashSession.user_id == current_user.id, CashSession.status == CashSessionStatus.OPEN ).first()`
    - Sugerencia: reemplazar por `get_tenant_scoped(db, CashSession, <id>, current_user)`
- **app/routers/cash.py:293** — model `CashSession`
    - Filter (id-only filter): `session = db.query(CashSession).filter(CashSession.id == session_id).first()`
    - Sugerencia: reemplazar por `get_tenant_scoped(db, CashSession, <id>, current_user)`
- **app/routers/cash.py:350** — model `SaleReturnModel`
    - Filter: `session_returns = db.query(SaleReturnModel).filter( SaleReturnModel.cash_session_id == session.id, SaleReturnModel.status == "APPROVED" ).all()`
    - Sugerencia: reemplazar por `get_tenant_scoped(db, SaleReturnModel, <id>, current_user)`
- **app/routers/cash.py:362** — model `SalesDocument`
    - Filter: `total_sales_query = db.query(func.sum(SalesDocument.total_amount)).filter( _session_filter, SalesDocument.status.in_(CASH_INCLUDED_STATUSES), ).scalar()`
    - Sugerencia: reemplazar por `get_tenant_scoped(db, SalesDocument, <id>, current_user)`
- **app/routers/cash.py:379** — model `Payment`
    - Filter: `last_payment = db.query(Payment.created_at).join(SalesDocument).filter( SalesDocument.seller_id == session.user_id, SalesDocument.branch_id == session.branch_id, Payment.created...`
    - Sugerencia: reemplazar por `get_tenant_scoped(db, Payment, <id>, current_user)`
- **app/routers/cash.py:514** — model `CashSession`
    - Filter: `session = db.query(CashSession).filter( CashSession.user_id == current_user.id, CashSession.branch_id == current_user.branch_id, CashSession.status == CashSessionStatus.OPEN ).f...`
    - Sugerencia: reemplazar por `get_tenant_scoped(db, CashSession, <id>, current_user)`
- **app/routers/cash.py:554** — model `CashSession`
    - Filter: `session = db.query(CashSession).filter( CashSession.user_id == current_user.id, CashSession.branch_id == current_user.branch_id, CashSession.status == CashSessionStatus.OPEN ).f...`
    - Sugerencia: reemplazar por `get_tenant_scoped(db, CashSession, <id>, current_user)`
- **app/routers/cash.py:594** — model `CashSession`
    - Filter (id-only filter): `session = db.query(CashSession).filter(CashSession.id == session_id).first()`
    - Sugerencia: reemplazar por `get_tenant_scoped(db, CashSession, <id>, current_user)`
- **app/routers/cash.py:598** — model `UserOrganization`
    - Filter: `user_org = db.query(UserOrganization.organization_id).filter( UserOrganization.user_id == current_user.id, UserOrganization.is_active == True ).first()`
    - Sugerencia: reemplazar por `get_tenant_scoped(db, UserOrganization, <id>, current_user)`
- **app/routers/cash.py:623** — model `CashAuditLog`
    - Filter: `rows = db.query(CashAuditLog).filter( CashAuditLog.session_id == session_id, ).order_by(CashAuditLog.id.asc()).all()`
    - Sugerencia: reemplazar por `get_tenant_scoped(db, CashAuditLog, <id>, current_user)`
- **app/routers/cash.py:730** — model `CashSession`
    - Filter: `sessions = db.query(CashSession).filter( CashSession.branch_id == target_branch, CashSession.opened_at >= day_start_utc, CashSession.opened_at <= day_end_utc, ).order_by(CashSes...`
    - Sugerencia: reemplazar por `get_tenant_scoped(db, CashSession, <id>, current_user)`
- **app/routers/cash.py:372** — model `SalesDocument`
    - Filter: `total_tickets = db.query(func.count(SalesDocument.id)).filter( _session_filter, SalesDocument.status.in_(CASH_INCLUDED_STATUSES), ).scalar() or 0`
    - Sugerencia: reemplazar por `get_tenant_scoped(db, SalesDocument, <id>, current_user)`
- **app/routers/cash.py:488** — model `CashSession`
    - Filter: `session = db.query(CashSession).filter( CashSession.id == session_id, CashSession.branch_id == current_user.branch_id, ).first()`
    - Sugerencia: reemplazar por `get_tenant_scoped(db, CashSession, <id>, current_user)`
- **app/routers/cash.py:495** — model `CashSession`
    - Filter: `session = db.query(CashSession).filter( CashSession.user_id == current_user.id, CashSession.branch_id == current_user.branch_id, CashSession.status == CashSessionStatus.OPEN ).f...`
    - Sugerencia: reemplazar por `get_tenant_scoped(db, CashSession, <id>, current_user)`
- **app/routers/cash.py:786** — model `SalesDocument`
    - Filter: `sales_total = db.query(func.sum(SalesDocument.total_amount)).filter( sales_filter, SalesDocument.status.in_(CASH_INCLUDED_STATUSES), ).scalar() or Decimal(0)`
    - Sugerencia: reemplazar por `get_tenant_scoped(db, SalesDocument, <id>, current_user)`
- **app/routers/cash.py:791** — model `SalesDocument`
    - Filter: `ticket_count = db.query(func.count(SalesDocument.id)).filter( sales_filter, SalesDocument.status.in_(CASH_INCLUDED_STATUSES), ).scalar() or 0`
    - Sugerencia: reemplazar por `get_tenant_scoped(db, SalesDocument, <id>, current_user)`
- **app/routers/cash.py:810** — model `SaleReturnModel`
    - Filter: `returns_count = db.query(func.count(SaleReturnModel.id)).filter( SaleReturnModel.cash_session_id == s.id, SaleReturnModel.status == "APPROVED", ).scalar() or 0`
    - Sugerencia: reemplazar por `get_tenant_scoped(db, SaleReturnModel, <id>, current_user)`

### `app/routers/platform/users.py`  (21 suspect)

- **app/routers/platform/users.py:125** — model `UserOrganization`
    - Filter: `assoc = db.query(UserOrganization).filter( UserOrganization.user_id == user.id, UserOrganization.is_active.is_(True), ).first()`
    - Sugerencia: reemplazar por `get_tenant_scoped(db, UserOrganization, <id>, current_user)`
- **app/routers/platform/users.py:59** — model `UserOrganization`
    - Filter: `assocs = db.query(UserOrganization).filter( UserOrganization.user_id.in_(user_ids), UserOrganization.is_active.is_(True), ).all()`
    - Sugerencia: reemplazar por `get_tenant_scoped(db, UserOrganization, <id>, current_user)`
- **app/routers/platform/users.py:209** — model `BranchAssignment`
    - Filter: `ba_count = db.query(BranchAssignment).filter( BranchAssignment.employee_id.in_(emp_ids) ).count()`
    - Sugerencia: reemplazar por `get_tenant_scoped(db, BranchAssignment, <id>, current_user)`
- **app/routers/platform/users.py:214** — model `UserOrganization`
    - Filter: `"organizations": db.query(UserOrganization).filter(UserOrganization.user_id == user_id).count(),`
    - Sugerencia: reemplazar por `get_tenant_scoped(db, UserOrganization, <id>, current_user)`
- **app/routers/platform/users.py:216** — model `SalesDocument`
    - Filter: `"sales_created": db.query(SalesDocument).filter( SalesDocument.seller_id == user_id, SalesDocument.doc_type != DocumentType.QUOTE, ).count(),`
    - Sugerencia: reemplazar por `get_tenant_scoped(db, SalesDocument, <id>, current_user)`
- **app/routers/platform/users.py:220** — model `CashSession`
    - Filter: `"cash_sessions": db.query(CashSession).filter(CashSession.user_id == user_id).count(),`
    - Sugerencia: reemplazar por `get_tenant_scoped(db, CashSession, <id>, current_user)`
- **app/routers/platform/users.py:221** — model `SalesDocument`
    - Filter: `"quotes_created": db.query(SalesDocument).filter( SalesDocument.seller_id == user_id, SalesDocument.doc_type == DocumentType.QUOTE, ).count(),`
    - Sugerencia: reemplazar por `get_tenant_scoped(db, SalesDocument, <id>, current_user)`
- **app/routers/platform/users.py:225** — model `Expense`
    - Filter: `"expenses_created": db.query(Expense).filter(Expense.user_id == user_id).count(),`
    - Sugerencia: reemplazar por `get_tenant_scoped(db, Expense, <id>, current_user)`
- **app/routers/platform/users.py:260** — model `UserOrganization`
    - Filter: `db.query(UserOrganization).filter(UserOrganization.user_id == user_id).delete( synchronize_session=False )`
    - Sugerencia: reemplazar por `get_tenant_scoped(db, UserOrganization, <id>, current_user)`
- **app/routers/platform/users.py:279** — model `Attendance`
    - Filter: `db.query(Attendance).filter(Attendance.created_by_user_id == user_id).update( {"created_by_user_id": None}, synchronize_session=False )`
    - Sugerencia: reemplazar por `get_tenant_scoped(db, Attendance, <id>, current_user)`
- **app/routers/platform/users.py:282** — model `Payment`
    - Filter: `db.query(Payment).filter(Payment.created_by_id == user_id).update( {"created_by_id": None}, synchronize_session=False )`
    - Sugerencia: reemplazar por `get_tenant_scoped(db, Payment, <id>, current_user)`
- **app/routers/platform/users.py:285** — model `SaleReturn`
    - Filter: `db.query(SaleReturn).filter(SaleReturn.supervisor_id == user_id).update( {"supervisor_id": None}, synchronize_session=False )`
    - Sugerencia: reemplazar por `get_tenant_scoped(db, SaleReturn, <id>, current_user)`
- **app/routers/platform/users.py:288** — model `Product`
    - Filter: `db.query(Product).filter(Product.created_by_user_id == user_id).update( {"created_by_user_id": None}, synchronize_session=False )`
    - Sugerencia: reemplazar por `get_tenant_scoped(db, Product, <id>, current_user)`
- **app/routers/platform/users.py:291** — model `InventoryMovement`
    - Filter: `db.query(InventoryMovement).filter(InventoryMovement.user_id == user_id).update( {"user_id": None}, synchronize_session=False )`
    - Sugerencia: reemplazar por `get_tenant_scoped(db, InventoryMovement, <id>, current_user)`
- **app/routers/platform/users.py:294** — model `AccountTransaction`
    - Filter: `db.query(AccountTransaction).filter(AccountTransaction.created_by_user_id == user_id).update( {"created_by_user_id": None}, synchronize_session=False )`
    - Sugerencia: reemplazar por `get_tenant_scoped(db, AccountTransaction, <id>, current_user)`
- **app/routers/platform/users.py:297** — model `ContainerLoadCalc`
    - Filter: `db.query(ContainerLoadCalc).filter(ContainerLoadCalc.created_by_user_id == user_id).update( {"created_by_user_id": None}, synchronize_session=False )`
    - Sugerencia: reemplazar por `get_tenant_scoped(db, ContainerLoadCalc, <id>, current_user)`
- **app/routers/platform/users.py:206** — model `Employee`
    - Filter: `emp_ids = [r[0] for r in db.query(Employee.id).filter(Employee.user_id == user_id).all()]`
    - Sugerencia: reemplazar por `get_tenant_scoped(db, Employee, <id>, current_user)`
- **app/routers/platform/users.py:267** — model `BranchAssignment`
    - Filter: `db.query(BranchAssignment).filter( BranchAssignment.employee_id.in_(emp_ids) ).delete(synchronize_session=False)`
    - Sugerencia: reemplazar por `get_tenant_scoped(db, BranchAssignment, <id>, current_user)`
- **app/routers/platform/users.py:270** — model `Attendance`
    - Filter: `db.query(Attendance).filter( Attendance.employee_id.in_(emp_ids) ).delete(synchronize_session=False)`
    - Sugerencia: reemplazar por `get_tenant_scoped(db, Attendance, <id>, current_user)`
- **app/routers/platform/users.py:274** — model `Employee`
    - Filter: `db.query(Employee).filter(Employee.user_id == user_id).update( {"user_id": None}, synchronize_session=False )`
    - Sugerencia: reemplazar por `get_tenant_scoped(db, Employee, <id>, current_user)`
- **app/routers/platform/users.py:265** — model `Employee`
    - Filter: `emp_ids = [r[0] for r in db.query(Employee.id).filter(Employee.user_id == user_id).all()]`
    - Sugerencia: reemplazar por `get_tenant_scoped(db, Employee, <id>, current_user)`

### `app/routers/platform/organizations.py`  (19 suspect)

- **app/routers/platform/organizations.py:173** — model `UserOrganization`
    - Filter: `still_has = db.query(UserOrganization).filter(UserOrganization.user_id == uid).count()`
    - Sugerencia: reemplazar por `get_tenant_scoped(db, UserOrganization, <id>, current_user)`
- **app/routers/platform/organizations.py:192** — model `Attendance`
    - Filter: `deleted["attendances"] = db.query(Attendance).filter( Attendance.employee_id.in_(employee_ids) ).delete(synchronize_session=False)`
    - Sugerencia: reemplazar por `get_tenant_scoped(db, Attendance, <id>, current_user)`
- **app/routers/platform/organizations.py:195** — model `BranchAssignment`
    - Filter: `deleted["branch_assignments"] = db.query(BranchAssignment).filter( BranchAssignment.employee_id.in_(employee_ids) ).delete(synchronize_session=False)`
    - Sugerencia: reemplazar por `get_tenant_scoped(db, BranchAssignment, <id>, current_user)`
- **app/routers/platform/organizations.py:199** — model `Employee`
    - Filter: `deleted["employees"] = db.query(Employee).filter( Employee.id.in_(employee_ids) ).delete(synchronize_session=False)`
    - Sugerencia: reemplazar por `get_tenant_scoped(db, Employee, <id>, current_user)`
- **app/routers/platform/organizations.py:276** — model `PurchaseOrderLine`
    - Filter: `deleted["purchase_order_lines"] = db.query(PurchaseOrderLine).filter( PurchaseOrderLine.purchase_order_id.in_(po_ids) ).delete(synchronize_session=False)`
    - Sugerencia: reemplazar por `get_tenant_scoped(db, PurchaseOrderLine, <id>, current_user)`
- **app/routers/platform/organizations.py:333** — model `ContainerLoadCalc`
    - Filter: `deleted["container_load_calcs"] = db.query(ContainerLoadCalc).filter( ContainerLoadCalc.container_type_id.in_(ct_ids) ).delete(synchronize_session=False)`
    - Sugerencia: reemplazar por `get_tenant_scoped(db, ContainerLoadCalc, <id>, current_user)`
- **app/routers/platform/organizations.py:345** — model `ShipmentItem`
    - Filter: `deleted["shipment_items"] = db.query(ShipmentItem).filter( ShipmentItem.shipment_id.in_(ship_ids) ).delete(synchronize_session=False)`
    - Sugerencia: reemplazar por `get_tenant_scoped(db, ShipmentItem, <id>, current_user)`
- **app/routers/platform/organizations.py:370** — model `TransferOrderLine`
    - Filter: `deleted["transfer_order_lines"] = db.query(TransferOrderLine).filter( TransferOrderLine.transfer_id.in_(transfer_ids) ).delete(synchronize_session=False)`
    - Sugerencia: reemplazar por `get_tenant_scoped(db, TransferOrderLine, <id>, current_user)`
- **app/routers/platform/organizations.py:374** — model `TransferOrder`
    - Filter: `deleted["transfer_orders"] = db.query(TransferOrder).filter( TransferOrder.id.in_(transfer_ids) ).delete(synchronize_session=False)`
    - Sugerencia: reemplazar por `get_tenant_scoped(db, TransferOrder, <id>, current_user)`
- **app/routers/platform/organizations.py:213** — model `SaleReturnItem`
    - Filter: `deleted["sale_return_items"] = db.query(SaleReturnItem).filter( SaleReturnItem.return_id.in_(return_ids) ).delete(synchronize_session=False)`
    - Sugerencia: reemplazar por `get_tenant_scoped(db, SaleReturnItem, <id>, current_user)`
- **app/routers/platform/organizations.py:251** — model `CashMovement`
    - Filter: `deleted["cash_movements"] = db.query(CashMovement).filter( CashMovement.session_id.in_(session_ids) ).delete(synchronize_session=False)`
    - Sugerencia: reemplazar por `get_tenant_scoped(db, CashMovement, <id>, current_user)`
- **app/routers/platform/organizations.py:255** — model `CashSession`
    - Filter: `deleted["cash_sessions"] = db.query(CashSession).filter( CashSession.id.in_(session_ids) ).delete(synchronize_session=False)`
    - Sugerencia: reemplazar por `get_tenant_scoped(db, CashSession, <id>, current_user)`
- **app/routers/platform/organizations.py:362** — model `TransferFulfillmentLine`
    - Filter: `deleted["transfer_fulfillment_lines"] = db.query(TransferFulfillmentLine).filter( TransferFulfillmentLine.fulfillment_id.in_(ful_ids) ).delete(synchronize_session=False)`
    - Sugerencia: reemplazar por `get_tenant_scoped(db, TransferFulfillmentLine, <id>, current_user)`
- **app/routers/platform/organizations.py:366** — model `TransferFulfillment`
    - Filter: `deleted["transfer_fulfillments"] = db.query(TransferFulfillment).filter( TransferFulfillment.id.in_(ful_ids) ).delete(synchronize_session=False)`
    - Sugerencia: reemplazar por `get_tenant_scoped(db, TransferFulfillment, <id>, current_user)`
- **app/routers/platform/organizations.py:179** — model `Employee`
    - Filter: `employee_ids = [r[0] for r in db.query(Employee.id).filter( Employee.base_branch_id.in_(branch_ids) ).all()]`
    - Sugerencia: reemplazar por `get_tenant_scoped(db, Employee, <id>, current_user)`
- **app/routers/platform/organizations.py:184** — model `Employee`
    - Filter: `extra_emp_ids = [r[0] for r in db.query(Employee.id).filter( Employee.user_id.in_(orphan_user_ids) ).all()]`
    - Sugerencia: reemplazar por `get_tenant_scoped(db, Employee, <id>, current_user)`
- **app/routers/platform/organizations.py:209** — model `SaleReturn`
    - Filter: `return_ids = [r[0] for r in db.query(SaleReturn.id).filter( SaleReturn.sale_id.in_(sale_ids) ).all()]`
    - Sugerencia: reemplazar por `get_tenant_scoped(db, SaleReturn, <id>, current_user)`
- **app/routers/platform/organizations.py:247** — model `CashSession`
    - Filter: `session_ids = [r[0] for r in db.query(CashSession.id).filter( CashSession.branch_id.in_(branch_ids) ).all()]`
    - Sugerencia: reemplazar por `get_tenant_scoped(db, CashSession, <id>, current_user)`
- **app/routers/platform/organizations.py:358** — model `TransferFulfillment`
    - Filter: `ful_ids = [r[0] for r in db.query(TransferFulfillment.id).filter( TransferFulfillment.transfer_id.in_(transfer_ids) ).all()]`
    - Sugerencia: reemplazar por `get_tenant_scoped(db, TransferFulfillment, <id>, current_user)`

### `app/crud/returns.py`  (13 suspect)

- **app/crud/returns.py:34** — model `SalesDocument`
    - Filter: `sale_q = db.query(SalesDocument).filter(SalesDocument.id == return_in.sale_id)`
    - Sugerencia: reemplazar por `get_tenant_scoped(db, SalesDocument, <id>, current_user)`
- **app/crud/returns.py:43** — model `SaleReturn`
    - Filter: `pending_q = db.query(SaleReturn).filter( SaleReturn.sale_id == sale.id, SaleReturn.status == "PENDING", )`
    - Sugerencia: reemplazar por `get_tenant_scoped(db, SaleReturn, <id>, current_user)`
- **app/crud/returns.py:55** — model `SaleReturnItem`
    - Filter: `previous_returns = db.query(SaleReturnItem).join(SaleReturn).filter( SaleReturn.sale_id == sale.id, SaleReturn.status == "APPROVED" ).all()`
    - Sugerencia: reemplazar por `get_tenant_scoped(db, SaleReturnItem, <id>, current_user)`
- **app/crud/returns.py:141** — model `SaleReturn`
    - Filter (id-only filter): `q = db.query(SaleReturn).filter(SaleReturn.id == return_id)`
    - Sugerencia: reemplazar por `get_tenant_scoped(db, SaleReturn, <id>, current_user)`
- **app/crud/returns.py:241** — model `SaleReturnItem`
    - Filter: `all_returned_items = db.query(SaleReturnItem).join(SaleReturn).filter( SaleReturn.sale_id == sale.id, SaleReturn.status == "APPROVED" ).all()`
    - Sugerencia: reemplazar por `get_tenant_scoped(db, SaleReturnItem, <id>, current_user)`
- **app/crud/returns.py:437** — model `SaleReturn`
    - Filter (id-only filter): `q = db.query(SaleReturn).filter(SaleReturn.id == return_id)`
    - Sugerencia: reemplazar por `get_tenant_scoped(db, SaleReturn, <id>, current_user)`
- **app/crud/returns.py:455** — model `SaleReturn`
    - Filter: `q = db.query(SaleReturn).filter(SaleReturn.sale_id == sale_id)`
    - Sugerencia: reemplazar por `get_tenant_scoped(db, SaleReturn, <id>, current_user)`
- **app/crud/returns.py:65** — model `SalesLineItem`
    - Filter: `sale_line = db.query(SalesLineItem).filter( SalesLineItem.document_id == sale.id, SalesLineItem.variant_id == item_in.variant_id ).first()`
    - Sugerencia: reemplazar por `get_tenant_scoped(db, SalesLineItem, <id>, current_user)`
- **app/crud/returns.py:99** — model `SalesLineItem`
    - Filter: `line_item = db.query(SalesLineItem).filter( SalesLineItem.variant_id == item_in.variant_id, SalesLineItem.document_id == sale.id ).first()`
    - Sugerencia: reemplazar por `get_tenant_scoped(db, SalesLineItem, <id>, current_user)`
- **app/crud/returns.py:287** — model `CashSession`
    - Filter: `active = db.query(CashSession.id).filter( CashSession.user_id == supervisor_id, CashSession.branch_id == db_return.branch_id, CashSession.status == CashSessionStatus.OPEN, ).fir...`
    - Sugerencia: reemplazar por `get_tenant_scoped(db, CashSession, <id>, current_user)`
- **app/crud/returns.py:204** — model `StockOnHand`
    - Filter: `stock = db.query(StockOnHand).filter( StockOnHand.branch_id == db_return.branch_id, StockOnHand.variant_id == item.variant_id ).first()`
    - Sugerencia: reemplazar por `get_tenant_scoped(db, StockOnHand, <id>, current_user)`
- **app/crud/returns.py:293** — model `CashSession`
    - Filter: `active = db.query(CashSession.id).filter( CashSession.id == db_return.cash_session_id, CashSession.status == CashSessionStatus.OPEN, ).first()`
    - Sugerencia: reemplazar por `get_tenant_scoped(db, CashSession, <id>, current_user)`
- **app/crud/returns.py:313** — model `CashSession`
    - Filter: `recent = db.query(CashSession.id).filter( CashSession.user_id == db_return.user_id, CashSession.branch_id == db_return.branch_id, ).order_by(CashSession.opened_at.desc()).first()`
    - Sugerencia: reemplazar por `get_tenant_scoped(db, CashSession, <id>, current_user)`

### `app/routers/hr.py`  (11 suspect)

- **app/routers/hr.py:25** — model `Employee`
    - Filter: `emp = db.query(Employee).filter(Employee.user_id == current_user.id).first()`
    - Sugerencia: reemplazar por `get_tenant_scoped(db, Employee, <id>, current_user)`
- **app/routers/hr.py:54** — model `Employee`
    - Filter: `emp = db.query(Employee).filter(Employee.user_id == current_user.id).first()`
    - Sugerencia: reemplazar por `get_tenant_scoped(db, Employee, <id>, current_user)`
- **app/routers/hr.py:100** — model `Employee`
    - Filter: `q = db.query(Employee).filter(Employee.base_branch_id.in_(branch_ids))`
    - Sugerencia: reemplazar por `get_tenant_scoped(db, Employee, <id>, current_user)`
- **app/routers/hr.py:113** — model `Employee`
    - Filter: `emp = db.query(Employee).filter( Employee.id == emp_id, Employee.base_branch_id.in_(branch_ids) ).first()`
    - Sugerencia: reemplazar por `get_tenant_scoped(db, Employee, <id>, current_user)`
- **app/routers/hr.py:130** — model `Employee`
    - Filter: `emp = db.query(Employee).filter( Employee.id == emp_id, Employee.base_branch_id.in_(branch_ids) ).first()`
    - Sugerencia: reemplazar por `get_tenant_scoped(db, Employee, <id>, current_user)`
- **app/routers/hr.py:153** — model `Employee`
    - Filter: `emp = db.query(Employee).filter( Employee.id == emp_id, Employee.base_branch_id.in_(branch_ids) ).first()`
    - Sugerencia: reemplazar por `get_tenant_scoped(db, Employee, <id>, current_user)`
- **app/routers/hr.py:177** — model `Employee`
    - Filter: `emp = db.query(Employee).filter( Employee.id == req.employee_id, Employee.base_branch_id.in_(branch_ids) ).first()`
    - Sugerencia: reemplazar por `get_tenant_scoped(db, Employee, <id>, current_user)`
- **app/routers/hr.py:185** — model `Attendance`
    - Filter: `open_session = db.query(Attendance).filter( Attendance.employee_id == req.employee_id, Attendance.check_out == None ).first()`
    - Sugerencia: reemplazar por `get_tenant_scoped(db, Attendance, <id>, current_user)`
- **app/routers/hr.py:211** — model `Attendance`
    - Filter: `attendance = db.query(Attendance).filter( Attendance.id == req.attendance_id, Attendance.branch_id.in_(branch_ids) ).first()`
    - Sugerencia: reemplazar por `get_tenant_scoped(db, Attendance, <id>, current_user)`
- **app/routers/hr.py:238** — model `Attendance`
    - Filter: `q = db.query(Attendance).filter( Attendance.branch_id.in_(branch_ids), func.date(Attendance.check_in) >= start_date, func.date(Attendance.check_in) <= end_date, )`
    - Sugerencia: reemplazar por `get_tenant_scoped(db, Attendance, <id>, current_user)`
- **app/routers/hr.py:81** — model `Employee`
    - Filter: `existing = db.query(Employee).filter(Employee.user_id == emp.user_id).first()`
    - Sugerencia: reemplazar por `get_tenant_scoped(db, Employee, <id>, current_user)`

### `app/routers/quotes.py`  (9 suspect)

- **app/routers/quotes.py:341** — model `SalesLineItem`
    - Filter: `db.query(SalesLineItem).filter(SalesLineItem.document_id == quote.id).delete()`
    - Sugerencia: reemplazar por `get_tenant_scoped(db, SalesLineItem, <id>, current_user)`
- **app/routers/quotes.py:491** — model `SalesDocument`
    - Filter: `base_query = db.query(SalesDocument).filter( SalesDocument.doc_type.in_([DocumentType.QUOTE, DocumentType.ORDER]), SalesDocument.branch_id == current_user.branch_id )`
    - Sugerencia: reemplazar por `get_tenant_scoped(db, SalesDocument, <id>, current_user)`
- **app/routers/quotes.py:215** — model `ProductVariant`
    - No filter at all: `variant = db.query(ProductVariant).get(line.variant_id)`
    - Sugerencia: reemplazar por `get_tenant_scoped(db, ProductVariant, <id>, current_user)`
- **app/routers/quotes.py:306** — model `SalesLineItem`
    - Filter: `db.query(SalesLineItem).filter(SalesLineItem.document_id == quote.id).delete()`
    - Sugerencia: reemplazar por `get_tenant_scoped(db, SalesLineItem, <id>, current_user)`
- **app/routers/quotes.py:429** — model `StockOnHand`
    - Filter: `stock = db.query(StockOnHand).filter( StockOnHand.variant_id == line.variant_id, StockOnHand.branch_id == current_user.branch_id ).first()`
    - Sugerencia: reemplazar por `get_tenant_scoped(db, StockOnHand, <id>, current_user)`
- **app/routers/quotes.py:461** — model `StockOnHand`
    - Filter: `stock = db.query(StockOnHand).filter( StockOnHand.variant_id == line.variant_id, StockOnHand.branch_id == current_user.branch_id ).first()`
    - Sugerencia: reemplazar por `get_tenant_scoped(db, StockOnHand, <id>, current_user)`
- **app/routers/quotes.py:497** — model `SalesDocument`
    - Filter: `total_amount = db.query(func.sum(SalesDocument.total_amount)).filter( SalesDocument.doc_type.in_([DocumentType.QUOTE, DocumentType.ORDER]), SalesDocument.branch_id == current_us...`
    - Sugerencia: reemplazar por `get_tenant_scoped(db, SalesDocument, <id>, current_user)`
- **app/routers/quotes.py:505** — model `SalesDocument`
    - Filter: `pending_amount = db.query(func.sum(SalesDocument.total_amount)).filter( SalesDocument.doc_type.in_([DocumentType.QUOTE, DocumentType.ORDER]), SalesDocument.branch_id == current_...`
    - Sugerencia: reemplazar por `get_tenant_scoped(db, SalesDocument, <id>, current_user)`
- **app/routers/quotes.py:236** — model `StockOnHand`
    - Filter: `stock_rec = db.query(StockOnHand).filter( StockOnHand.variant_id == variant.id, StockOnHand.branch_id == current_user.branch_id ).first()`
    - Sugerencia: reemplazar por `get_tenant_scoped(db, StockOnHand, <id>, current_user)`

### `app/routers/logistics.py`  (7 suspect)

- **app/routers/logistics.py:46** — model `ContainerType`
    - No filter at all: `return db.query(ContainerType).all()`
    - Sugerencia: reemplazar por `get_tenant_scoped(db, ContainerType, <id>, current_user)`
- **app/routers/logistics.py:66** — model `BoxType`
    - No filter at all: `return db.query(BoxType).all()`
    - Sugerencia: reemplazar por `get_tenant_scoped(db, BoxType, <id>, current_user)`
- **app/routers/logistics.py:92** — model `ContainerType`
    - No filter at all: `container = db.query(ContainerType).get(req.container_type_id)`
    - Sugerencia: reemplazar por `get_tenant_scoped(db, ContainerType, <id>, current_user)`
- **app/routers/logistics.py:93** — model `BoxType`
    - No filter at all: `box = db.query(BoxType).get(req.box_type_id)`
    - Sugerencia: reemplazar por `get_tenant_scoped(db, BoxType, <id>, current_user)`
- **app/routers/logistics.py:288** — model `ShipmentItem`
    - Filter: `item = db.query(ShipmentItem).filter( ShipmentItem.id == item_id, ShipmentItem.shipment_id == id ).first()`
    - Sugerencia: reemplazar por `get_tenant_scoped(db, ShipmentItem, <id>, current_user)`
- **app/routers/logistics.py:39** — model `ContainerType`
    - No filter at all: `if db.query(ContainerType).count() == 0:`
    - Sugerencia: reemplazar por `get_tenant_scoped(db, ContainerType, <id>, current_user)`
- **app/routers/logistics.py:60** — model `BoxType`
    - No filter at all: `if db.query(BoxType).count() == 0:`
    - Sugerencia: reemplazar por `get_tenant_scoped(db, BoxType, <id>, current_user)`

### `app/routers/products/import_export.py`  (7 suspect)

- **app/routers/products/import_export.py:117** — model `StockOnHand`
    - Filter: `s = db.query(StockOnHand).filter( StockOnHand.variant_id == v.id, StockOnHand.branch_id == target_branch_id ).first()`
    - Sugerencia: reemplazar por `get_tenant_scoped(db, StockOnHand, <id>, current_user)`
- **app/routers/products/import_export.py:530** — model `ProductBranchStatus`
    - Filter: `existing_status = db.query(ProductBranchStatus).filter( ProductBranchStatus.variant_id == variant.id, ProductBranchStatus.branch_id == bid ).first()`
    - Sugerencia: reemplazar por `get_tenant_scoped(db, ProductBranchStatus, <id>, current_user)`
- **app/routers/products/import_export.py:552** — model `StockOnHand`
    - Filter: `stock_record = db.query(StockOnHand).filter( StockOnHand.variant_id == variant.id, StockOnHand.branch_id == target_branch_id ).first()`
    - Sugerencia: reemplazar por `get_tenant_scoped(db, StockOnHand, <id>, current_user)`
- **app/routers/products/import_export.py:596** — model `PackagingUnit`
    - Filter: `db.query(PackagingUnit).filter(PackagingUnit.variant_id == variant.id).delete()`
    - Sugerencia: reemplazar por `get_tenant_scoped(db, PackagingUnit, <id>, current_user)`
- **app/routers/products/import_export.py:590** — model `ProductPrice`
    - Filter: `db.query(ProductPrice).filter(ProductPrice.variant_id == variant.id).delete()`
    - Sugerencia: reemplazar por `get_tenant_scoped(db, ProductPrice, <id>, current_user)`
- **app/routers/products/import_export.py:593** — model `ProductPrice`
    - Filter: `db.query(ProductPrice).filter(ProductPrice.variant_id == variant.id).update({ProductPrice.linked_package_id: None})`
    - Sugerencia: reemplazar por `get_tenant_scoped(db, ProductPrice, <id>, current_user)`
- **app/routers/products/import_export.py:599** — model `ProductPrice`
    - Filter: `db.query(ProductPrice).filter(ProductPrice.variant_id == variant.id).delete()`
    - Sugerencia: reemplazar por `get_tenant_scoped(db, ProductPrice, <id>, current_user)`

### `app/routers/portal.py`  (6 suspect)

- **app/routers/portal.py:56** — model `Customer`
    - Filter: `customers = db.query(Customer).filter(Customer.email == email).all()`
    - Sugerencia: reemplazar por `get_tenant_scoped(db, Customer, <id>, current_user)`
- **app/routers/portal.py:100** — model `Customer`
    - Filter: `query = db.query(Customer).filter(Customer.email == email)`
    - Sugerencia: reemplazar por `get_tenant_scoped(db, Customer, <id>, current_user)`
- **app/routers/portal.py:130** — model `Customer`
    - Filter: `customers = db.query(Customer).filter(Customer.email == email).all()`
    - Sugerencia: reemplazar por `get_tenant_scoped(db, Customer, <id>, current_user)`
- **app/routers/portal.py:151** — model `SalesDocument`
    - Filter: `quotes = db.query(SalesDocument).filter( SalesDocument.customer_id.in_(customer_ids), SalesDocument.doc_type.in_(["COTIZACION", "QUOTE"]) # Adjust based on actual Enum ).order_b...`
    - Sugerencia: reemplazar por `get_tenant_scoped(db, SalesDocument, <id>, current_user)`
- **app/routers/portal.py:179** — model `Customer`
    - Filter: `query = db.query(Customer).filter(Customer.email == email)`
    - Sugerencia: reemplazar por `get_tenant_scoped(db, Customer, <id>, current_user)`
- **app/routers/portal.py:190** — model `AccountTransaction`
    - Filter: `txs = db.query(AccountTransaction).filter( AccountTransaction.customer_id.in_(customer_ids) ).order_by(desc(AccountTransaction.created_at)).limit(50).all()`
    - Sugerencia: reemplazar por `get_tenant_scoped(db, AccountTransaction, <id>, current_user)`

### `app/routers/sales.py`  (6 suspect)

- **app/routers/sales.py:469** — model `ProductBranchStatus`
    - Filter: `_pbs_rows = db.query(ProductBranchStatus).filter( ProductBranchStatus.variant_id.in_(_all_vids), ProductBranchStatus.branch_id == current_user.branch_id, ).all()`
    - Sugerencia: reemplazar por `get_tenant_scoped(db, ProductBranchStatus, <id>, current_user)`
- **app/routers/sales.py:1013** — model `SaleReturn`
    - Filter: `returns = db.query(SaleReturn).filter( SaleReturn.sale_id == sale_id, SaleReturn.status == "APPROVED" ).options(joinedload(SaleReturn.items)).all()`
    - Sugerencia: reemplazar por `get_tenant_scoped(db, SaleReturn, <id>, current_user)`
- **app/routers/sales.py:357** — model `CashSession`
    - Filter: `active_session = db.query(CashSession).filter( CashSession.user_id == current_user.id, CashSession.branch_id == current_user.branch_id, CashSession.status == CashSessionStatus.O...`
    - Sugerencia: reemplazar por `get_tenant_scoped(db, CashSession, <id>, current_user)`
- **app/routers/sales.py:668** — model `_CashSession`
    - Filter: `active_cash = db.query(_CashSession.id).filter( _CashSession.user_id == current_user.id, _CashSession.branch_id == current_user.branch_id, _CashSession.status == _CSStatus.OPEN,...`
    - Sugerencia: reemplazar por `get_tenant_scoped(db, _CashSession, <id>, current_user)`
- **app/routers/sales.py:396** — model `SalesLineItem`
    - Filter: `db.query(SalesLineItem).filter(SalesLineItem.document_id == existing_sale.id).delete()`
    - Sugerencia: reemplazar por `get_tenant_scoped(db, SalesLineItem, <id>, current_user)`
- **app/routers/sales.py:397** — model `Payment`
    - Filter: `db.query(Payment).filter(Payment.sales_document_id == existing_sale.id).delete()`
    - Sugerencia: reemplazar por `get_tenant_scoped(db, Payment, <id>, current_user)`

### `app/routers/branches.py`  (4 suspect)

- **app/routers/branches.py:282** — model `SalesDocument`
    - Filter: `has_sales = db.query(SalesDocument).filter(SalesDocument.branch_id == branch_id).first()`
    - Sugerencia: reemplazar por `get_tenant_scoped(db, SalesDocument, <id>, current_user)`
- **app/routers/branches.py:286** — model `CashSession`
    - Filter: `has_sessions = db.query(CashSession).filter(CashSession.branch_id == branch_id).first()`
    - Sugerencia: reemplazar por `get_tenant_scoped(db, CashSession, <id>, current_user)`
- **app/routers/branches.py:291** — model `StockOnHand`
    - Filter: `db.query(StockOnHand).filter(StockOnHand.branch_id == branch_id).delete()`
    - Sugerencia: reemplazar por `get_tenant_scoped(db, StockOnHand, <id>, current_user)`
- **app/routers/branches.py:292** — model `ProductBranchStatus`
    - Filter: `db.query(ProductBranchStatus).filter(ProductBranchStatus.branch_id == branch_id).delete()`
    - Sugerencia: reemplazar por `get_tenant_scoped(db, ProductBranchStatus, <id>, current_user)`

### `app/routers/printer.py`  (3 suspect)

- **app/routers/printer.py:185** — model `SalesDocument`
    - Filter: `sale = db.query(SalesDocument).filter(SalesDocument.id == req.order_id).first()`
    - Sugerencia: reemplazar por `get_tenant_scoped(db, SalesDocument, <id>, current_user)`
- **app/routers/printer.py:237** — model `SalesDocument`
    - Filter (id-only filter): `sale = db.query(SalesDocument).options( joinedload(SalesDocument.lines).joinedload(SalesLineItem.variant), joinedload(SalesDocument.payments), selectinload(SalesDocument.returns...`
    - Sugerencia: reemplazar por `get_tenant_scoped(db, SalesDocument, <id>, current_user)`
- **app/routers/printer.py:297** — model `SalesDocument`
    - Filter (id-only filter): `sale = db.query(SalesDocument).options( joinedload(SalesDocument.lines).joinedload(SalesLineItem.variant), joinedload(SalesDocument.branch), selectinload(SalesDocument.returns)....`
    - Sugerencia: reemplazar por `get_tenant_scoped(db, SalesDocument, <id>, current_user)`

### `app/routers/products/_shared.py`  (3 suspect)

- **app/routers/products/_shared.py:143** — model `ProductBranchStatus`
    - Filter: `db.query(ProductBranchStatus) .filter( ProductBranchStatus.variant_id == v.id, ProductBranchStatus.branch_id == real_branch_id ) .first()`
    - Sugerencia: reemplazar por `get_tenant_scoped(db, ProductBranchStatus, <id>, current_user)`
- **app/routers/products/_shared.py:179** — model `ProductBranchStatus`
    - Filter: `p_read.branch_statuses = db.query(ProductBranchStatus).filter( ProductBranchStatus.variant_id == v.id ).all()`
    - Sugerencia: reemplazar por `get_tenant_scoped(db, ProductBranchStatus, <id>, current_user)`
- **app/routers/products/_shared.py:127** — model `StockOnHand`
    - Filter: `db.query(StockOnHand) .filter( StockOnHand.variant_id == v.id, StockOnHand.branch_id == real_branch_id ) .first()`
    - Sugerencia: reemplazar por `get_tenant_scoped(db, StockOnHand, <id>, current_user)`

### `app/routers/products/core.py`  (3 suspect)

- **app/routers/products/core.py:858** — model `ProductBranchStatus`
    - Filter: `branch_status = db.query(ProductBranchStatus).filter( ProductBranchStatus.variant_id == variant.id, ProductBranchStatus.branch_id == current_user.branch_id ).first()`
    - Sugerencia: reemplazar por `get_tenant_scoped(db, ProductBranchStatus, <id>, current_user)`
- **app/routers/products/core.py:923** — model `ProductPrice`
    - Filter: `db.query(ProductPrice).filter(ProductPrice.variant_id == v.id).update( {ProductPrice.linked_package_id: None}, synchronize_session=False )`
    - Sugerencia: reemplazar por `get_tenant_scoped(db, ProductPrice, <id>, current_user)`
- **app/routers/products/core.py:974** — model `ProductBranchStatus`
    - Filter: `db.query(ProductBranchStatus).filter( ProductBranchStatus.variant_id == variant.id ).delete()`
    - Sugerencia: reemplazar por `get_tenant_scoped(db, ProductBranchStatus, <id>, current_user)`

### `app/routers/products/stats.py`  (3 suspect)

- **app/routers/products/stats.py:229** — model `StockOnHand`
    - Filter: `db.query(StockOnHand) .join(ProductBranchStatus, _pbs_join_branch) .filter(StockOnHand.branch_id == branch_id)`
    - Sugerencia: reemplazar por `get_tenant_scoped(db, StockOnHand, <id>, current_user)`
- **app/routers/products/stats.py:76** — model `ProductVariant`
    - Filter: `total_skus = db.query(func.count(ProductVariant.id)).filter( ProductVariant.product_id.in_(visible_product_ids.select()), ).scalar() or 0`
    - Sugerencia: reemplazar por `get_tenant_scoped(db, ProductVariant, <id>, current_user)`
- **app/routers/products/stats.py:113** — model `ProductVariant`
    - Filter: `no_branch = db.query(func.count(ProductVariant.id)).filter( ProductVariant.product_id.in_(visible_product_ids.select()), ~ProductVariant.id.in_(no_branch_subq.select()), ).scala...`
    - Sugerencia: reemplazar por `get_tenant_scoped(db, ProductVariant, <id>, current_user)`

### `app/services/cash_reconciliation.py`  (3 suspect)

- **app/services/cash_reconciliation.py:111** — model `SalesDocument`
    - Filter: `db.query(SalesDocument) .filter( session_sales_filter(session), SalesDocument.status.in_(CASH_INCLUDED_STATUSES), ) .all()`
    - Sugerencia: reemplazar por `get_tenant_scoped(db, SalesDocument, <id>, current_user)`
- **app/services/cash_reconciliation.py:176** — model `CashMovement`
    - Filter: `db.query(CashMovement) .filter(CashMovement.session_id == session.id) .order_by(CashMovement.created_at.asc()) .all()`
    - Sugerencia: reemplazar por `get_tenant_scoped(db, CashMovement, <id>, current_user)`
- **app/services/cash_reconciliation.py:158** — model `Payment`
    - Filter: `db.query(func.sum(Payment.amount)) .join(SalesDocument) .filter( Payment.method == PaymentMethod.CASH, session_sales_filter(session), SalesDocument.status.in_(CASH_INCLUDED_STAT...`
    - Sugerencia: reemplazar por `get_tenant_scoped(db, Payment, <id>, current_user)`

### `app/crud/crm.py`  (2 suspect)

- **app/crud/crm.py:6** — model `Customer`
    - Filter (id-only filter): `q = db.query(Customer).filter(Customer.id == customer_id)`
    - Sugerencia: reemplazar por `get_tenant_scoped(db, Customer, <id>, current_user)`
- **app/crud/crm.py:12** — model `Customer`
    - Filter: `q = db.query(Customer).filter(Customer.tax_id == tax_id)`
    - Sugerencia: reemplazar por `get_tenant_scoped(db, Customer, <id>, current_user)`

### `app/crud/products.py`  (2 suspect)

- **app/crud/products.py:408** — model `ProductVariant`
    - Filter: `return db.query(ProductVariant).filter(ProductVariant.sku == sku).first()`
    - Sugerencia: reemplazar por `get_tenant_scoped(db, ProductVariant, <id>, current_user)`
- **app/crud/products.py:483** — model `ProductBranchStatus`
    - Filter: `db.query(ProductBranchStatus) .filter( ProductBranchStatus.variant_id == variant_id, ProductBranchStatus.branch_id == target_branch_id, ) .first()`
    - Sugerencia: reemplazar por `get_tenant_scoped(db, ProductBranchStatus, <id>, current_user)`

### `app/modules/auth/router.py`  (2 suspect)

- **app/modules/auth/router.py:74** — model `UserOrganization`
    - Filter: `user_org = db.query(UserOrganization).filter(UserOrganization.user_id == user.id).first()`
    - Sugerencia: reemplazar por `get_tenant_scoped(db, UserOrganization, <id>, current_user)`
- **app/modules/auth/router.py:129** — model `UserOrganization`
    - Filter: `is_org_admin = db.query(UserOrganization).filter( UserOrganization.user_id == current_user.id, UserOrganization.org_role == "ADMIN" ).first()`
    - Sugerencia: reemplazar por `get_tenant_scoped(db, UserOrganization, <id>, current_user)`

### `app/routers/platform/reports.py`  (2 suspect)

- **app/routers/platform/reports.py:711** — model `Product`
    - Filter (id-only filter): `product = db.query(Product).filter(Product.id == product_id).first()`
    - Sugerencia: reemplazar por `get_tenant_scoped(db, Product, <id>, current_user)`
- **app/routers/platform/reports.py:949** — model `Customer`
    - Filter (id-only filter): `customer = db.query(Customer).filter(Customer.id == customer_id).first()`
    - Sugerencia: reemplazar por `get_tenant_scoped(db, Customer, <id>, current_user)`

### `app/services/branch_dashboard.py`  (2 suspect)

- **app/services/branch_dashboard.py:107** — model `CashSession`
    - Filter: `self.db.query(CashSession) .filter( CashSession.user_id == self.user.id, CashSession.branch_id == self.branch_id, CashSession.closed_at.is_(None), ) .order_by(CashSession.opened...`
    - Sugerencia: reemplazar por `get_tenant_scoped(db, CashSession, <id>, current_user)`
- **app/services/branch_dashboard.py:120** — model `CashSession`
    - Filter: `self.db.query(CashSession) .filter( CashSession.branch_id == self.branch_id, CashSession.closed_at.isnot(None), CashSession.closed_at >= today_start, ) .order_by(CashSession.clo...`
    - Sugerencia: reemplazar por `get_tenant_scoped(db, CashSession, <id>, current_user)`

### `app/modules/customers/router.py`  (1 suspect)

- **app/modules/customers/router.py:386** — model `Payment`
    - Filter: `payments_sum = db.query(func.sum(Payment.amount)).filter( Payment.sales_document_id == doc.id ).scalar() or 0`
    - Sugerencia: reemplazar por `get_tenant_scoped(db, Payment, <id>, current_user)`

### `app/routers/platform/control_tower.py`  (1 suspect)

- **app/routers/platform/control_tower.py:136** — model `CashSession`
    - Filter: `db.query(func.count(CashSession.id)) .filter(CashSession.status == CashSessionStatus.OPEN) .scalar()`
    - Sugerencia: reemplazar por `get_tenant_scoped(db, CashSession, <id>, current_user)`

### `app/routers/platform/feature_flags.py`  (1 suspect)

- **app/routers/platform/feature_flags.py:244** — model `OrgFeatureOverride`
    - Filter: `db.query(OrgFeatureOverride) .filter(OrgFeatureOverride.flag_key == key) .delete(synchronize_session=False)`
    - Sugerencia: reemplazar por `get_tenant_scoped(db, OrgFeatureOverride, <id>, current_user)`

### `app/routers/platform/stats.py`  (1 suspect)

- **app/routers/platform/stats.py:100** — model `SalesDocument`
    - Filter: `db.query(SalesDocument.organization_id) .filter(SalesDocument.created_at >= today_start, SalesDocument.status != DocumentStatus.CANCELLED) .distinct().count()`
    - Sugerencia: reemplazar por `get_tenant_scoped(db, SalesDocument, <id>, current_user)`

### `app/routers/products/branch_status.py`  (1 suspect)

- **app/routers/products/branch_status.py:492** — model `ProductBranchStatus`
    - Filter: `existing = db.query(ProductBranchStatus).filter( ProductBranchStatus.variant_id == variant_id, ProductBranchStatus.branch_id == target_branch_id, ).first()`
    - Sugerencia: reemplazar por `get_tenant_scoped(db, ProductBranchStatus, <id>, current_user)`

### `app/routers/products/search.py`  (1 suspect)

- **app/routers/products/search.py:106** — model `ProductVariant`
    - Filter: `db.query(ProductVariant) .join(Product, Product.id == ProductVariant.product_id) .filter(Product.id.in_(visible_pids)) .filter( or_( ProductVariant.sku.ilike(s), Product.name.il...`
    - Sugerencia: reemplazar por `get_tenant_scoped(db, ProductVariant, <id>, current_user)`

## Top 10 files to fix first

| Rank | File | Suspect queries |
|-----:|------|----------------:|
| 1 | `app/routers/platform/branches.py` | 30 |
| 2 | `app/routers/cash.py` | 23 |
| 3 | `app/routers/platform/users.py` | 21 |
| 4 | `app/routers/platform/organizations.py` | 19 |
| 5 | `app/crud/returns.py` | 13 |
| 6 | `app/routers/hr.py` | 11 |
| 7 | `app/routers/quotes.py` | 9 |
| 8 | `app/routers/logistics.py` | 7 |
| 9 | `app/routers/products/import_export.py` | 7 |
| 10 | `app/routers/portal.py` | 6 |

## Unclassified queries (manual review)

These query chains couldn't be classified statically (dynamic model selection, subqueries, multi-model selects, etc.). A human should eyeball each one.

### `app/crud/returns.py`
- L169: prior_refunded = db.query( func.coalesce(func.sum(SaleReturnItem.refund_amount), Decimal("0")) ).join(SaleReturn).filter( SaleReturn.sale_id == sale.id, SaleReturn.status == "AP...
- L362: prior_refunded_pretax = db.query( func.coalesce(func.sum(SaleReturnItem.refund_amount), 0) ).join(SaleReturn).filter( SaleReturn.sale_id == sale.id, SaleReturn.status == "APPROV...

### `app/routers/cash.py`
- L320: payment_stats = db.query( Payment.method, func.count(Payment.id).label("count"), func.sum(Payment.amount).label("total") ).join(SalesDocument).filter( _session_filter, SalesDocu...
- L336: invoice_stats = db.query( func.sum(SalesDocument.tax_amount).label("taxes"), func.sum(SalesDocument.subtotal).label("subtotal") ).filter( _session_filter, SalesDocument.status.i...
- L796: payment_stats = db.query( Payment.method, func.sum(Payment.amount).label("total") ).join(SalesDocument).filter( sales_filter, SalesDocument.status.in_(CASH_INCLUDED_STATUSES), )...

### `app/routers/platform/alerts.py`
- L57: q = db.query(PlatformAlert, Organization.name).outerjoin( Organization, Organization.id == PlatformAlert.organization_id )
- L104: total_row = db.query( sqlfunc.count(PlatformAlert.id).label("total"), sqlfunc.sum(sql_case((PlatformAlert.resolved_at.is_(None), 1), else_=0)).label("active"), sqlfunc.sum(sql_c...
- L203: active_orgs = db.query(Organization.id, Organization.name).filter( Organization.status == "ACTIVE" ).all()

### `app/routers/platform/api_keys.py`
- L105: query = db.query(ApiKey, Organization.name).outerjoin( Organization, Organization.id == ApiKey.organization_id )

### `app/routers/platform/branches.py`
- L247: ful_ids = [r[0] for r in db.query(TransferFulfillment.id).filter(ful_filter).all()]

### `app/routers/platform/control_tower.py`
- L71: db.query( func.date_trunc("hour", SalesDocument.created_at).label("hour"), func.coalesce(func.sum(SalesDocument.total_amount), 0).label("revenue"), func.count(SalesDocument.id)....
- L142: db.query( CashSession.id.label("id"), CashSession.branch_id.label("branch_id"), CashSession.organization_id.label("organization_id"), Organization.name.label("org_name"), CashSe...
- L305: db.query( cast(SalesDocument.created_at, Date).label("d"), func.coalesce(func.sum(SalesDocument.total_amount), 0).label("rev"), ) .filter( SalesDocument.created_at >= spark_star...
- L356: db.query( Organization.id.label("oid"), Organization.name.label("oname"), Organization.industry_type.label("industry"), func.coalesce(func.sum(SalesDocument.total_amount), 0).la...
- L205: db.query(func.coalesce(func.sum(SalesDocument.total_amount), 0)) .filter( SalesDocument.created_at >= current_start, SalesDocument.created_at <= now_utc, SalesDocument.status !=...
- L215: db.query(func.coalesce(func.sum(SalesDocument.total_amount), 0)) .filter( SalesDocument.created_at >= prior_start, SalesDocument.created_at < prior_end, SalesDocument.status != ...

### `app/routers/platform/feature_flags.py`
- L132: db.query(OrgFeatureOverride.flag_key, _func.count(OrgFeatureOverride.id)) .filter(OrgFeatureOverride.flag_key.in_(keys)) .group_by(OrgFeatureOverride.flag_key) .all()
- L310: db.query(OrgFeatureOverride, Organization.name) .outerjoin(Organization, Organization.id == OrgFeatureOverride.organization_id) .filter(OrgFeatureOverride.flag_key == key) .orde...

### `app/routers/platform/modules.py`
- L156: db.query(OrganizationModule.module_key, func.count(OrganizationModule.organization_id)) .filter(OrganizationModule.is_enabled == True) .group_by(OrganizationModule.module_key) ....
- L192: db.query(OrganizationModule, Organization) .join(Organization, Organization.id == OrganizationModule.organization_id) .filter(OrganizationModule.module_key == module_key, Organi...

### `app/routers/platform/reports.py`
- L192: db.query( SaleReturnItem.variant_id.label("variant_id"), func.coalesce(func.sum(SaleReturnItem.quantity), 0).label("returned_qty"), ) .join(SaleReturn, SaleReturnItem.return_id ...
- L206: db.query( SaleReturn.branch_id.label("branch_id"), func.count(distinct(SaleReturn.id)).label("return_count"), ) .join(SalesDocument, SaleReturn.sale_id == SalesDocument.id)
- L257: db.query( Product.id.label("product_id"), Product.name.label("name"), ProductVariant.sku.label("sku"), Brand.name.label("brand"), Department.name.label("department"), units_sold...
- L716: db.query( SalesDocument.id.label("document_id"), SalesDocument.folio.label("folio"), SalesDocument.series.label("series"), SalesDocument.created_at.label("created_at"), Organiza...
- L797: db.query( Product.id.label("product_id"), Product.name.label("name"), ProductVariant.sku.label("sku"), units_sold, revenue, ) .select_from(SalesLineItem) .join(SalesDocument, Sa...
- L827: db.query( User.id.label("user_id"), User.full_name.label("full_name"), User.username.label("username"), transactions, sell_revenue, ) .select_from(SalesDocument) .join(User, Sal...
- L884: db.query( SalesDocument.id.label("document_id"), SalesDocument.folio.label("folio"), SalesDocument.series.label("series"), SalesDocument.created_at.label("created_at"), SalesDoc...
- L955: db.query( Payment.sales_document_id.label("doc_id"), func.min(Payment.method).label("method"), ) .group_by(Payment.sales_document_id) .subquery()
- L964: db.query( SalesDocument.id.label("document_id"), SalesDocument.folio.label("folio"), SalesDocument.series.label("series"), SalesDocument.created_at.label("created_at"), SalesDoc...
- L1067: db.query( ProductVariant.product_id.label("product_id"), func.coalesce(func.sum(ret_sub.c.returned_qty), 0).label("returned_qty"), ) .outerjoin(ret_sub, ret_sub.c.variant_id == ...
- L1138: db.query( Branch.id.label("branch_id"), Branch.name.label("name"), Branch.city.label("city"), Organization.id.label("org_id"), Organization.name.label("org_name"), transactions,...
- L1223: db.query( User.id.label("user_id"), User.full_name.label("full_name"), User.username.label("username"), User.role.label("role"), Branch.id.label("branch_id"), Branch.name.label(...
- L1310: db.query( Customer.id.label("customer_id"), Customer.name.label("customer_name"), ticket_count, total_rev, last_purchase, first_purchase, ) .select_from(SalesDocument) .join(Cus...
- L306: db.query( ProductVariant.product_id.label("product_id"), func.coalesce(func.sum(ret_sub.c.returned_qty), 0).label("returned_qty"), ) .outerjoin(ret_sub, ret_sub.c.variant_id == ...
- L405: db.query( Branch.id.label("branch_id"), Branch.name.label("name"), Branch.city.label("city"), Organization.id.label("org_id"), Organization.name.label("org_name"), transactions,...
- L512: db.query( User.id.label("user_id"), User.full_name.label("full_name"), User.username.label("username"), User.role.label("role"), Branch.id.label("branch_id"), Branch.name.label(...
- L631: db.query( Customer.id.label("customer_id"), Customer.name.label("customer_name"), ticket_count, total_rev, last_purchase, first_purchase, ) .select_from(SalesDocument) .join(Cus...
- L1077: for r in db.query(ret_per_product.c.product_id, ret_per_product.c.returned_qty).all()
- L1159: for r in db.query(ret_sub.c.branch_id, ret_sub.c.return_count).all()
- L319: for r in db.query(ret_per_product.c.product_id, ret_per_product.c.returned_qty).all()
- L428: for r in db.query(ret_sub.c.branch_id, ret_sub.c.return_count).all()

### `app/routers/platform/stats.py`
- L79: sales_data = db.query(func.sum(SalesDocument.total_amount), func.count(SalesDocument.id)).first()
- L139: revenue_trend = db.query( cast(SalesDocument.created_at, Date).label('date'), func.sum(SalesDocument.total_amount).label('revenue'), func.count(SalesDocument.id).label('count') ...
- L150: org_trend = db.query( cast(Organization.created_at, Date).label('date'), func.count(Organization.id).label('count') ).filter( cast(Organization.created_at, Date) >= start_date )...
- L180: distribution = db.query( Organization.industry_type, func.count(Organization.id).label('count') ).group_by( Organization.industry_type ).all()
- L200: top_orgs = db.query( Organization.id, Organization.name, func.sum(SalesDocument.total_amount).label('total_revenue'), func.count(SalesDocument.id).label('transaction_count') ).j...
- L314: active_tenants_rows = db.query( cast(SalesDocument.created_at, Date).label("d"), func.count(func.distinct(SalesDocument.organization_id)).label("cnt"), ).filter( SalesDocument.c...
- L344: active_branches_rows = db.query( cast(SalesDocument.created_at, Date).label("d"), func.count(func.distinct(SalesDocument.branch_id)).label("cnt"), ).filter( SalesDocument.create...
- L504: sales_rows = db.query( cast(SalesDocument.created_at, Date).label("d"), func.coalesce(func.sum(SalesDocument.total_amount), 0).label("rev"), func.count(SalesDocument.id).label("...
- L515: signup_rows = db.query( cast(Organization.created_at, Date).label("d"), func.count(Organization.id).label("cnt"), ).filter( Organization.created_at >= start, Organization.create...
- L557: org_totals = db.query( Organization.id.label("oid"), func.coalesce(func.sum(SalesDocument.total_amount), 0).label("rev"), ).outerjoin( SalesDocument, (SalesDocument.organization...
- L574: rows = db.query( bucket_expr, func.count().label("cnt"), func.coalesce(func.sum(org_totals.c.rev), 0).label("rev_total"), ).group_by("bucket").all()
- L604: org_counts = db.query( Organization.id.label("oid"), func.count(Branch.id).label("bcnt"), ).outerjoin( Branch, (Branch.organization_id == Organization.id) & (Branch.is_active ==...
- L619: rows = db.query( bucket_expr, func.count().label("cnt"), ).group_by("bucket").all()
- L652: rows = db.query( Branch.id.label("bid"), Branch.name.label("bname"), Organization.id.label("oid"), Organization.name.label("oname"), func.coalesce(func.sum(SalesDocument.total_a...
- L713: top_rows = db.query( Branch.id.label("bid"), Branch.name.label("bname"), Organization.id.label("oid"), Organization.name.label("oname"), func.coalesce(func.sum(SalesDocument.tot...
- L740: hour_rows = db.query( SalesDocument.branch_id.label("bid"), extract("hour", SalesDocument.created_at).label("hr"), func.count(SalesDocument.id).label("cnt"), ).filter( SalesDocu...
- L801: rows = db.query( Product.id.label("pid"), Product.name.label("pname"), ProductVariant.id.label("vid"), ProductVariant.variant_name.label("vname"), ProductVariant.sku.label("sku"...
- L955: signup_rows = db.query( Organization.id, Organization.created_at ).filter( Organization.created_at >= oldest_start, Organization.created_at < newest_end, ).all()
- L1033: rows = db.query( func.extract("dow", SalesDocument.created_at).label("dow"), func.extract("hour", SalesDocument.created_at).label("hour"), func.count(SalesDocument.id).label("cn...
- L84: db.query(func.coalesce(func.sum(SalesDocument.total_amount), 0)) .filter(SalesDocument.created_at >= today_start, SalesDocument.status != DocumentStatus.CANCELLED) .scalar() or 0
- L90: db.query(func.coalesce(func.sum(SalesDocument.total_amount), 0)) .filter(SalesDocument.created_at >= yesterday_start, SalesDocument.created_at < today_start, SalesDocument.statu...
- L251: rows = db.query( cast(SalesDocument.created_at, Date).label("d"), func.coalesce(func.sum(SalesDocument.total_amount), 0).label("rev"), func.count(SalesDocument.id).label("cnt"),...
- L328: active_tenants_value = db.query( func.count(func.distinct(SalesDocument.organization_id)) ).filter( SalesDocument.created_at >= start, SalesDocument.created_at <= end, SalesDocu...
- L335: active_tenants_prior = db.query( func.count(func.distinct(SalesDocument.organization_id)) ).filter( SalesDocument.created_at >= prior_start, SalesDocument.created_at < mid, Sale...
- L358: active_branches_value = db.query( func.count(func.distinct(SalesDocument.branch_id)) ).filter( SalesDocument.created_at >= start, SalesDocument.created_at <= end, SalesDocument....
- L365: active_branches_prior = db.query( func.count(func.distinct(SalesDocument.branch_id)) ).filter( SalesDocument.created_at >= prior_start, SalesDocument.created_at < mid, SalesDocu...
- L398: orgs_with_sale = db.query( func.count(func.distinct(SalesDocument.organization_id)) ).filter( SalesDocument.created_at >= start, SalesDocument.created_at <= end, SalesDocument.s...
- L416: orgs_with_sale_prior = db.query( func.count(func.distinct(SalesDocument.organization_id)) ).filter( SalesDocument.created_at >= prior_start, SalesDocument.created_at < mid, Sale...
- L880: rows = db.query( Payment.method.label("method"), func.coalesce(func.sum(Payment.amount), 0).label("revenue"), func.count(Payment.id).label("count"), ).join( SalesDocument, Sales...
- L388: db.query(func.coalesce(func.sum(SalesDocument.total_amount), 0)) .filter( SalesDocument.created_at >= mrr_prior_start, SalesDocument.created_at < mrr_prior_end, SalesDocument.st...

### `app/routers/products/audit.py`
- L58: db.query(PlatformAuditLog, UserModel.username) .outerjoin(UserModel, PlatformAuditLog.actor_user_id == UserModel.id) .filter( PlatformAuditLog.entity_type == "ProductBranchStatu...

### `app/routers/products/packaging.py`
- L45: query = db.query( PackagingUnit, Product.name.label("product_name"), ProductVariant.sku.label("sku"), StockOnHand.qty_on_hand ).join(ProductVariant, PackagingUnit.variant_id == ...

### `app/routers/products/reports.py`
- L126: db.query( ProductVariant.cost, func.coalesce(func.sum(StockOnHand.qty_on_hand), 0).label("total_qty"), ) .join(Product, Product.id == ProductVariant.product_id) .outerjoin(Stock...
- L156: db.query(StockOnHand.variant_id, StockOnHand.qty_on_hand, StockOnHand.is_active) .filter(StockOnHand.variant_id.in_(variant_ids), StockOnHand.branch_id == target_branch_id) .all()

### `app/routers/products/search.py`
- L208: all_pbs = db.query( ProductBranchStatus.variant_id, ProductBranchStatus.branch_id, ProductBranchStatus.is_active_pos ).filter( ProductBranchStatus.variant_id.in_(variant_ids) )....

### `app/routers/reports.py`
- L252: today_sales_stats = db.query( func.count(SalesDocument.id).label("count"), func.sum(SalesDocument.total_amount).label("total") ).filter(*sales_filters).first()
- L270: trend_data = db.query( func.date(SalesDocument.created_at).label("day"), func.sum(SalesDocument.total_amount).label("total") ).filter(*sales_filters).group_by("day").order_by("d...
- L288: hourly_data = db.query( func.to_char(_mx(SalesDocument.created_at), 'HH24').label("hour"), func.sum(SalesDocument.total_amount).label("total") ).filter(*sales_filters).group_by(...
- L302: payments_data = db.query( Payment.method, func.sum(Payment.amount).label("total") ).join(SalesDocument).filter(*sales_filters).group_by(Payment.method).all()
- L311: top_products = db.query( SalesLineItem.description, func.sum(SalesLineItem.quantity).label("qty") ).join(SalesDocument).filter(*sales_filters).group_by(SalesLineItem.description...
- L335: recent_sales = db.query(SalesDocument).filter(*sales_filters).order_by(desc(SalesDocument.created_at)).limit(10).all()
- L367: prev_trend_data = db.query( func.date(_mx(SalesDocument.created_at)).label("day"), func.sum(SalesDocument.total_amount).label("total"), ).filter(*prev_filters).group_by("day").o...
- L382: heatmap_raw = db.query( func.extract('dow', _mx(SalesDocument.created_at)).label("dow"), func.to_char(_mx(SalesDocument.created_at), 'HH24').label("hour"), func.sum(SalesDocumen...
- L505: rows = db.query( func.to_char(_mx(SalesDocument.created_at), 'HH24').label("hour"), func.sum(SalesDocument.total_amount).label("amount"), func.count(SalesDocument.id).label("tic...
- L558: global_kpis = db.query( func.sum(SalesDocument.total_amount).label("sales"), func.count(SalesDocument.id).label("tickets"), func.sum(func.coalesce(SalesDocument.total_amount,0) ...
- L595: cancel_stats = db.query( func.count(SalesDocument.id).label("count"), func.sum(SalesDocument.total_amount).label("total") ).filter(*cancel_filters).first()
- L618: payments = db.query( Payment.method, func.sum(Payment.amount).label("total") ).join(SalesDocument).filter(*sales_filters).group_by(Payment.method).all()
- L625: hourly_data = db.query( func.to_char(_mx(SalesDocument.created_at), 'HH24').label("hour"), func.sum(SalesDocument.total_amount).label("total") ).filter(*sales_filters).group_by(...
- L719: top_revenue = db.query( Product.id.label("product_id"), Product.name.label("name"), func.sum(SalesLineItem.quantity).label("qty"), func.sum(SalesLineItem.total_line).label("reve...
- L857: sales_stats = db.query( func.count(SalesDocument.id).label("count"), func.sum(SalesDocument.total_amount).label("total"), ).filter(*sales_filters).first()
- L881: top_products = db.query( SalesLineItem.description, func.sum(SalesLineItem.quantity).label("qty"), ).join(SalesDocument).filter(*sales_filters)\ .group_by(SalesLineItem.descript...
- L890: hourly_data = db.query( func.to_char(_mx(SalesDocument.created_at), 'HH24').label("hour"), func.sum(SalesDocument.total_amount).label("total"), ).filter(*sales_filters).group_by...
- L263: pending_count = db.query(func.count(SalesDocument.id)).filter(*pending_filters).scalar() or 0
- L266: draft_count = db.query(func.count(SalesDocument.id)).filter(*draft_filters).scalar() or 0
- L350: unique_skus_sold = db.query(func.count(func.distinct(SalesLineItem.variant_id)))\ .join(SalesDocument).filter(*sales_filters).scalar() or 0
- L378: prev_count = db.query(func.count(SalesDocument.id)).filter(*prev_filters).scalar() or 0
- L404: tp_trend_rows = db.query( SalesLineItem.description, func.date(_mx(SalesDocument.created_at)).label("day"), func.sum(SalesLineItem.quantity).label("qty"), ).join(SalesDocument)....
- L579: prev_total = db.query(func.sum(SalesDocument.total_amount)).filter(*prev_sales_filters).scalar() or 0
- L609: returns_total = db.query(func.sum(SaleReturn.total_refunded)).filter(*return_filters).scalar() or 0
- L613: total_items = db.query(func.sum(SalesLineItem.quantity)).join(SalesDocument)\ .filter(*sales_filters).scalar() or 0
- L636: db.query( SalesDocument.branch_id, func.coalesce(func.sum(SalesDocument.total_amount), 0), ).filter(*sales_filters).group_by(SalesDocument.branch_id).all()
- L645: open_sessions_rows = db.query( CashSession.branch_id, CashSession.id, CashSession.opened_at, User.username, ).join(User, CashSession.user_id == User.id).filter( CashSession.stat...
- L877: unique_skus = db.query(func.count(func.distinct(SalesLineItem.variant_id)))\ .join(SalesDocument).filter(*sales_filters).scalar() or 0

### `app/services/branch_dashboard.py`
- L73: self.db.query(CashSession.id, CashSession.opened_at) .filter( CashSession.user_id == self.user.id, CashSession.branch_id == self.branch_id, CashSession.closed_at.is_(None), ) .o...
- L335: self.db.query(CashSession.id, CashSession.difference) .filter( CashSession.branch_id == self.branch_id, CashSession.closed_at.isnot(None), ) .order_by(CashSession.closed_at.desc...


---
**Summary:** scanned 747 queries — 454 safe, 187 suspect, 106 unclassified. Coverage: 60.8%.
